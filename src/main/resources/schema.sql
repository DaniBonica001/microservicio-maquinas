CREATE TABLE machines (
                          machineId BIGINT AUTO_INCREMENT PRIMARY KEY,
                          name VARCHAR(255),
                          description VARCHAR(255),
                          quantity INT
);